template(name='breadcrumbs')
  ol.breadcrumb
    li
      a(href='/') {{_ 'layouts.appBody.home'}}
    +each parents
      li
        a(href='/note/{{_id}}/{{shareKey}}')
          +emojione title
    if focusedTitle
      li 
        +emojione focusedTitle
